<li>
  <div class="first">
    <%= image_tag(service.avatar.url(:square_60)) %>
    <h3><%= link_to service.title, service %></h3>
  </div>

  <div>
    <strong>Originators:</strong> <%= service.originators.map {|o| link_to o.name, o } * ", " %>
  </div>

  <div class="last">
    <%= link_to "Send a message", "#" %>
  </div>
</li>